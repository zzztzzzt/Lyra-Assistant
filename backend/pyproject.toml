[project]
name = "lyra-assistant"
version = "0.1.0"
description = "Next Generation AI Color Palette / Web Gradients"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.2",
    "python-dotenv>=1.2.1",
]
